Test 
Might need a CardDeck class that aggregates the Card class.
* Will have a constructor that takes in a size and initializes a number of Cards and inserts them into a list.
* May need helper function that initializes the cards and returns a list.
* May need getImages(numImages) function that grabs a random set of images from the images/ folder.
* Need to initialize two cards for every image.
* randomizeCardOrder() function
